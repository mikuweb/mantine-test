import { createStyles } from "@mantine/core";

export const useStyles = createStyles((theme) => ({
  container: {
    width: "80%",
    maxWidth: "400px",
    margin: "0 auto",
    padding: "20px 24px",
    display: "flex",
    flexDirection: "column",
  },
}));
